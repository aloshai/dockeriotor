import { DockeriotorContext } from "..";

export function startCommand(ctx: DockeriotorContext) {
  ctx.reply(
    "👋 Welcome to Dockeriotor. You can learn about Dockeriotor with the commands below."
  );
}
